interface:
  display_name: "Publish Plugin"
  short_description: "Preview, package, and publish verified Pluxx releases"
  default_prompt: "Use $pluxx-publish-plugin to prepare a verified release plan and publish only after I approve the exact external action."
