interface:
  display_name: "Migrate Plugin"
  short_description: "Bring an existing Claude, Cursor, Codex, or OpenCode plugin into Pluxx"
  default_prompt: "Use Pluxx to migrate this existing host-native plugin into one maintained source project, then tell me what translated cleanly and what still needs cleanup."
