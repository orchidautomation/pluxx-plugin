---
description: Refresh an MCP-derived Pluxx project while preserving custom work
argument-hint: "[mcp-source-override optional]"
---

Use the `pluxx-maintain-plugin` skill.

Arguments: $ARGUMENTS

Preview `pluxx sync --dry-run --json`, review added, updated, removed, and preserved content, then apply only when the impact is understood. Return the sync delta, preserved custom work, validation result, and any refinement or installed-state follow-up.
