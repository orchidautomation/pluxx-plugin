---
description: "Review preserve, translate, degrade, and drop decisions across Claude, Cursor, Codex, and OpenCode"
argument-hint: "[target subset optional]"
---

Use the Pluxx host translation review workflow.

Arguments: $ARGUMENTS

## What To Do

1. Use the `pluxx-translate-hosts` skill.
2. Inspect the current scaffold and the configured compiler buckets.
3. Return the preserve / translate / degrade / drop story by host.
4. Call out anything that still feels weaker than a native plugin should.
5. Recommend the smallest next improvement.
