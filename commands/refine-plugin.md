---
description: Refine a valid Pluxx scaffold into a product-shaped, host-honest plugin
argument-hint: "[context-or-focus optional]"
---

Use the `pluxx-refine-plugin` skill.

Arguments: $ARGUMENTS

Treat the argument as product context or a focus such as taxonomy, instructions, examples, or scaffold review. Keep the refinement source-first, preserve managed/custom boundaries, re-run deterministic checks, and return what improved plus what still needs proof.
