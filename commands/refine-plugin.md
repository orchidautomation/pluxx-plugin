---
description: Refine a scaffold into a product-shaped, host-honest plugin
argument-hint: "[context-or-focus optional]"
---

Use the Pluxx refinement workflow.

Arguments: $ARGUMENTS

## What To Do

1. Use the `pluxx-refine-plugin` skill.
2. Treat the argument as context or focus:
   - website/docs/local context
   - taxonomy quality
   - instruction quality
   - host translation concerns
   - overall scaffold review
3. Keep this as one coordinated refinement pass instead of a list of separate subcommands.
4. Return what changed, what still feels weak, and whether the next step is proof or publish.
